# validationConfig_ScenarioMIP_IMAGE

Configuration file for ScenarioMIP energy validation with model-native
regions.

*Columns that are empty in this config and therefore not shown:*
`model`, `ref_period`
